<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="LugaresVisitados.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:LugaresVisitados"
    Title="LugaresVisitados">

    <ShellContent Title="Inicio" ContentTemplate="{DataTemplate local:MainPage}" />

    <!-- Páginas de navegación -->
    <ShellContent Route="agregar" ContentTemplate="{DataTemplate local:AgregarLugar}" />
    <ShellContent Route="editar" ContentTemplate="{DataTemplate local:EditarLugar}" />


</Shell>
